diff -Nupr src.orig/arch/x86/kvm/x86.c src/arch/x86/kvm/x86.c
--- src.orig/arch/x86/kvm/x86.c	2017-01-30 15:06:15.105012239 -0500
+++ src/arch/x86/kvm/x86.c	2017-01-30 15:07:00.891274163 -0500
@@ -1411,12 +1411,20 @@ static void kvmclock_reset(struct kvm_vc
 	vcpu->arch.pv_time_enabled = false;
 }
 
+void kpatch_kvm_x86_foo(void)
+{
+	if (!jiffies)
+		printk("kpatch kvm x86 foo\n");
+}
+
 int kvm_set_msr_common(struct kvm_vcpu *vcpu, struct msr_data *msr_info)
 {
 	bool pr = false;
 	u32 msr = msr_info->index;
 	u64 data = msr_info->data;
 
+	kpatch_kvm_x86_foo();
+
 	switch (msr) {
 	case MSR_EFER:
 		return set_efer(vcpu, data);
