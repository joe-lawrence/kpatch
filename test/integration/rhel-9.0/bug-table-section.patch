diff -Nupr src.orig/fs/proc/proc_sysctl.c src/fs/proc/proc_sysctl.c
--- src.orig/fs/proc/proc_sysctl.c	2021-03-10 14:11:26.601110422 -0500
+++ src/fs/proc/proc_sysctl.c	2021-03-10 14:11:27.619111574 -0500
@@ -307,6 +307,8 @@ static void start_unregistering(struct c
 
 static struct ctl_table_header *sysctl_head_grab(struct ctl_table_header *head)
 {
+	if (jiffies == 0)
+		printk("kpatch-test: testing __bug_table section changes\n");
 	BUG_ON(!head);
 	spin_lock(&sysctl_lock);
 	if (!use_table(head))
